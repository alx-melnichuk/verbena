import { DatepickerExtTags } from './datepicker-ext-tags';

describe('DatepickerExtTags', () => {
  it('should create an instance', () => {
    const directive = new DatepickerExtTags();
    expect(directive).toBeTruthy();
  });
});
