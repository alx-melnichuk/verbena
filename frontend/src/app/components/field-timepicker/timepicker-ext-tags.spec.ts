import { TimepickerExtTags } from './timepicker-ext-tags';

describe('TimepickerExtTags', () => {
  it('should create an instance', () => {
    const directive = new TimepickerExtTags();
    expect(directive).toBeTruthy();
  });
});
