import { ViewItemListByPage } from './view-item-list-by-page';

describe('ViewItemListByPage', () => {
  it('should create an instance', () => {
    const directive = new ViewItemListByPage();
    expect(directive).toBeTruthy();
  });
});
