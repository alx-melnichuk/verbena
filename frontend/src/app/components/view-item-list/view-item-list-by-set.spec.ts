import { ViewItemListBySet } from './view-item-list-by-set';

describe('ViewItemListBySet', () => {
  it('should create an instance', () => {
    const directive = new ViewItemListBySet();
    expect(directive).toBeTruthy();
  });
});
