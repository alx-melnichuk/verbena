import { FieldDragAndDrop } from './field-drag-and-drop';

describe('FieldDragAndDrop', () => {
  it('should create an instance', () => {
    const directive = new FieldDragAndDrop();
    expect(directive).toBeTruthy();
  });
});
