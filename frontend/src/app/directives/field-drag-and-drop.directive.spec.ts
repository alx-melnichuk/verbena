import { FieldDragAndDropDirective } from './field-drag-and-drop.directive';

describe('FieldDragAndDropDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldDragAndDropDirective();
    expect(directive).toBeTruthy();
  });
});
