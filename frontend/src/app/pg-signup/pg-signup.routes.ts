import { Routes } from '@angular/router';

import { PgSignupComponent } from './pg-signup.component';

export const PG_SIGNUP_ROUTES: Routes = [
    {
        path: '',
        component: PgSignupComponent,
    },
];
